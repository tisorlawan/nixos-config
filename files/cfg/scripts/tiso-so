#!/usr/bin/env nu

let content = (
    open ~/.gittoken
    | lines
    | where $it =~ 'h6Mh'
    | str join "\n"
)

if ($env.WAYLAND_DISPLAY | is-empty) {
    $content | xclip -selection clipboard
} else {
    $content | wl-copy
}
