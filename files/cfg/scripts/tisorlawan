#!/usr/bin/env nu

let content = (
    open ~/.gittoken
    | lines
    | where $it =~ 'zvsu'
    | str join "\n"
)

if ($env.WAYLAND_DISPLAY | is-empty) {
    $content | xclip -selection clipboard
} else {
    $content | wl-copy
}
